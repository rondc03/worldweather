spring:
  application:
    name: worldweather
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
      username: ${CONFIG_SERVER_USER:admin}
      password: ${CONFIG_SERVER_PASSWORD:}
      fail-fast: true
